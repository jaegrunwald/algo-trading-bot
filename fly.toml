# fly.toml app configuration file generated for algo-trading-bot on 2026-06-01T20:33:41Z
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'algo-trading-bot'
primary_region = 'ams'

[build]
  dockerfile = 'Dockerfile.fly.dashboard'

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = 'off'
  auto_start_machines = true
  min_machines_running = 1
  processes = ['app']

  [[http_service.checks]]
    interval = '30s'
    timeout = '5s'
    grace_period = '45s'
    method = 'get'
    path = '/'

[[vm]]
  memory = '512mb'
  cpu_kind = 'shared'
  cpus = 1
  memory_mb = 256
